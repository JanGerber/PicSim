<Window x:Class="PicSimulator.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PicSimulator.Views"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="PICSim" Height="800" Width="1200">
    <Grid RenderTransformOrigin="0.492,0.531" Margin="0,10,2,-300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="272*"/>
            <ColumnDefinition Width="113*"/>
            <ColumnDefinition Width="384*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height=".1*"/>
            <RowDefinition Height=".6*"/>
            <RowDefinition Height=".3*"/>
        </Grid.RowDefinitions>
        <Button x:Name="OpenFile" Content="Datei öffnen" Grid.Row="0" Grid.Column="0" Margin="0,0,245,0"/>
        <Label x:Name="FileName" Grid.Row="2" Grid.Column="1" Content="{Binding FileNameContent}" Margin="0,0,2,0"/>
        <Button x:Name="StartProgramm" Content="Start" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Width="158" Height="55"/>
        <Button x:Name="StopProgramm" Content="Stop" Grid.Column="1" HorizontalAlignment="Left" Height="55" Margin="0,52,0,0" VerticalAlignment="Top" Width="158"/>
        <Button x:Name="ResetProgramm" Content="Zurücksetzen" HorizontalAlignment="Left" Height="55" Margin="0,22,0,0" Grid.Row="1" VerticalAlignment="Top" Width="158" Grid.Column="1"/>
        <Button x:Name="StepProgramm" Content="Schritt vorwärts" HorizontalAlignment="Left" Height="55" Margin="0,77,0,0" Grid.Row="1" VerticalAlignment="Top" Width="158" Grid.Column="1"/>
        <TextBlock x:Name="AusgabeFeld"  HorizontalAlignment="Left"  Margin="0,428,0,0" TextWrapping="Wrap" Text="{Binding Dateiname}" VerticalAlignment="Top" Height="36" Width="670" Grid.Row="1" Grid.ColumnSpan="2"/>

        <DockPanel Grid.Column="0" Grid.Row="1" Height="auto" Width="auto" LastChildFill="True">
            <ListView ItemsSource="{Binding OpcodesObj}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="{Binding Value.Background}" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView >
                        <GridViewColumn Header="Break" >
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Value.Breakpoint}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Value.ProgrammCounter}" Header="Zeile" Width="100" />
                        <GridViewColumn DisplayMemberBinding="{Binding Value.BefehlsName}" Header="Befehl" Width="100" />
                        <GridViewColumn DisplayMemberBinding="{Binding Value.Parameter1}" Header="Parameter 1" Width="100" />
                        <GridViewColumn DisplayMemberBinding="{Binding Value.Parameter2}" Header="Parameter 2" Width="100" />
                    </GridView>
                </ListView.View>

            </ListView>
        </DockPanel>
        <DockPanel  Grid.Column="2" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBox Grid.Column="0" Grid.Row="0"   Text="Register 1:" Grid.ColumnSpan="2" />
                <TextBox Grid.Column="1" Grid.Row="0"   Text="{Binding Speicher.Register[1]}" />
                <TextBox Grid.Column="0" Grid.Row="1"   Text="Register 129:" Grid.ColumnSpan="2" />
                <TextBox Grid.Column="1" Grid.Row="1"   Text="{Binding Speicher.Register[129]}" />
            </Grid>

        </DockPanel>
    </Grid>

</Window>
